# Threatbase — Python dependencies
# Install with: pip install -r requirements.txt

# HTTP client — feed downloads, retries, timeout handling
requests>=2.32.0
aiohttp>=3.9.0

# S3 compatible storage uploads
boto3>=1.34.0


