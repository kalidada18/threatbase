import React from "react"; export class ErrorBoundary extends React.Component<any, any> { constructor(props: any) { super(props); this.state = { hasError: false, error: null }; } static getDerivedStateFromError(error: any) { return { hasError: true, error }; } render() { if (this.state.hasError) { return <div style={{padding: "50px", color: "red", background: "black"}}><h1>React Crash</h1><pre>{String(this.state.error?.stack || this.state.error)}</pre><p style={{color:"#94a3b8", margin:"16px 0 12px"}}>A stale file from an app update is the usual cause — reloading fixes it.</p><button onClick={() => window.location.reload()} style={{background:"#cf1733", color:"#fff", border:"0", borderRadius:"8px", padding:"10px 18px", fontWeight:700, cursor:"pointer"}}>Reload</button><a href="/" style={{color:"#94a3b8", marginLeft:"16px"}}>Back to home</a></div>; } return this.props.children; } }
